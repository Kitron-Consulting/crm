# Data files — never commit your contacts (JSON or SQLite), copies or backups
crm_data.json
crm_real*.json
*.jsonbak
*.db
*.db-journal

# Session scratchpad (local seeds, screenshots, throwaway test deps)
scratchpad/

# Demo recording source files
demo.sh
*.cast
demo_runner.py

# Test setup script
test_setup.sh

# Python
__pycache__/
*.pyc
.venv/

# PyInstaller build artifacts
build/
dist/
*.spec

# Generated at release-build time from the git tag
crm/_version.py

# PyApp scratch build output
.pyapp-build/

# Local S3 creds for smoke testing — never commit
.s3-creds.env

# macOS
.DS_Store

# web UI (Svelte/Vite): deps + built bundle
web/node_modules/
crm/static/
